{
  "name": "aleph",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build_all": "electron-packager . --platform=all --arch=x64",
    "build_win": "electron-packager . --platform=win32 --arch=x64",
    "build_linux": "electron-packager . --platform=linux --arch=x64",
    "build_mac": "electron-packager . --platform=darwin --arch=x64"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^2.0.9",
    "electron-packager": "^12.2.0",
    "electron-rebuild": "^1.8.2",
    "grunt": "^1.0.3"
  },
  "dependencies": {
    "easymidi": "^1.0.1",
    "electron-debug": "^2.0.0",
    "p5": "^0.7.2"
  }
}
